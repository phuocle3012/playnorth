import HomePageServerComponent from "@/app/home-page/HomePageServerComponent";

const Root = async () => {
  return (
    <main>
        <HomePageServerComponent/>
    </main>
  );
};

export default Root;
